import Banner from '../components/Banner.tsx';
import BookCard from '../components/BookCard.tsx';

const Home = () => {
    return (
        <main>
            <Banner />
            <BookCard />
        </main>
    );
};

export default Home;
